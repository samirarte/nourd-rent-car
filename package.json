{
  "name": "nourd-rent-car",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "postbuild": "node scripts/patch-wrangler.mjs",
    "preview": "astro preview",
    "astro": "astro",
    "pages:deploy": "npx wrangler pages deploy ./dist --project-name=nourd-rent-car",
    "deploy": "npm run build && npx wrangler deploy --config dist/server/wrangler.json",
    "deploy:list": "npx wrangler deployments list"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.1.7",
    "@libsql/client": "^0.17.4",
    "@tailwindcss/vite": "^4.3.3",
    "astro": "^7.1.6",
    "tailwindcss": "^4.3.3"
  },
  "devDependencies": {
    "@astrojs/node": "^11.0.3",
    "wrangler": "^4.119.0"
  }
}
